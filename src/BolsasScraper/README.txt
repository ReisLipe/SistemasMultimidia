Web Scraper que gera um arquivo csv com uma lista de chamadas para bolsas de pesquisa do site IPEA.

Para rodar o arquivo é necessário instalar via pip os pacotes no arquivo "requirements", só ativar a venv e instalar via pip.